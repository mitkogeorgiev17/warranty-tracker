import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mitko.warranty.vault',
  appName: 'warranty-vault-ui',
  webDir: 'dist'
};

export default config;
